#include <stdio.h>
    int main() {
        int idade;

        // se a 'idade' for maior que 18, então imprime a mensagem
        idade = 20;
       

        if (idade >= 18){
            //bloco de código a ser executado, caso nao tem '{}' apenas a proxima linha é executada.
            printf("voce é maior de idade\n");
        }
        
        

        return 0;
    }